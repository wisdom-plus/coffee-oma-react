setup:
  config:
    NODE_ENV: production
    NPM_CONFIG_PRODUCTION: false
build:
  docker:
    web: Dockerfile.web
  config:
    WORKDIR: frontend
    API_URL: "https://coffee-oma-backend.herokuapp.com"
    NODE_ENV: production
run:
  web: yarn global add serve && serve -s build
